import Foundation
import UIKit

import Quick
import Nimble
import Spry
import Spry_Nimble

class RequestSpec: QuickSpec {
    override func spec() {
        describe("Request") {
            it("should be a real test") {
                expect(true).to(beTrue())
            }
        }
    }
}
